<div class="card">
<%= form_with(model: note) do |form| %>
  <% if note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(note.errors.count, "error") %> prohibited this note from being saved:</h2>

      <ul>
        <% note.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
    <div class="card-body">
      <div class="card-body">
  <div class="field form-group">
    <%= form.label 'Enter the note text' %>
    <%= form.text_field :note_text, class:"form-control" %>
  </div>

  <div class="actions">
    <%= form.submit "Create note", class: 'btn btn-secondary' %>
  </div>
<% end %>
  </div>
  </div>
</div>